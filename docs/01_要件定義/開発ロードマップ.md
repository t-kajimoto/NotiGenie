# NotiGenie 開発ロードマップ (Updated 2026-02)

このドキュメントは、NotiGenie開発の作業順序とタスクを定義します。

---

## Phase 0: 開発環境構築・セットアップ (完了)

開発に着手するための準備段階です。

- [x] **[★★☆☆☆] Raspberry PiのOSセットアップとネットワーク接続**
- [x] **[★☆☆☆☆] PythonおよびDockerのインストール**
- [x] **[★☆☆☆☆] プロジェクト用のGitリポジトリを作成し、Obsidianと連携**
- [x] **[★★☆☆☆] Notion APIのセットアップと接続情報の取得**
  - `NotionAdapter`によるAPI連携を実装済み。
- [x] **[★★☆☆☆] NotionMCPの仕様を確認し、基本的な操作を試す**
  - 公式ライブラリ(`notion-client`)を直接使用する方針に変更。
- [x] **[★☆☆☆☆] Gemini APIキーを取得し、サンプルコードで動作確認**
  - `GeminiAdapter`、`ProcessMessageUseCase`による実装完了。
- [x] **[★★☆☆☆] Google Cloud Speech-to-Text APIを有効化し、動作確認**
  - `raspberry_pi`クライアントにて実装済み。
- [x] **[★☆☆☆☆] テキストを音声に変換するライブラリ（gTTSなど）を選定し、動作確認**
  - `tts_interface.py`による抽象化と実装済み（VoiceVox/AquesTalk対応）。

---

## Phase 1: MVP（コア機能）開発 (開発中)

アプリケーションの核となる、最低限の機能を実装する段階です。

### Step 1.1: 基本的な音声入出力 (完了)

- [x] **[★★☆☆☆] 物理ボタンの押下を検知するスクリプトを作成**
- [x] **[★★☆☆☆] マイクから音声データを録音し、ファイルとして保存する機能を実装**
- [x] **[★★★☆☆] 録音された音声ファイルをSpeech-to-Text APIに送信し、テキストに変換する機能を実装**
- [x] **[★★☆☆☆] 任意のテキストを音声ファイルに変換し、再生する機能を実装**

### Step 1.2: クリーンアーキテクチャとAI連携 (完了)

- [x] **[★★★★☆] クリーンアーキテクチャに基づいたディレクトリ構造と、主要なクラスのひな形を作成**
  - `cloud_functions/core`配下にEntities, UseCases, Interfacesを配置。
- [x] **[★★★★☆] ユーザーの発話から、その意図とパラメータを抽出するよう、Gemini APIに指示するプロンプトを設計**
  - `cloud_functions/prompts/system_instruction.md`にて定義済み。
- [x] **[★★★☆☆] 抽出した意図とパラメータを基に、Notionを操作するロジックを実装**
  - `GeminiAdapter`のツール呼び出し機能と`NotionAdapter`により実現。

### Step 1.3: コア機能の実装と連携 (完了)

- [x] **[★★★★☆] Step 1.1と1.2を連携させ、一連の流れを実装**
  - Raspberry PiからCloud Functionsへのリクエストフローを確立。
- [x] **[★★★☆☆] 同様に、「作ったもの（料理在庫）の保存」機能を実装**
  - 汎用的なDB選択ロジックにより、スキーマ定義(`schemas.yaml`)を追加するだけで対応可能。

### Step 1.4: GUIの実装 (保留/変更)

- [ ] **[★★☆☆☆] GUIライブラリ（Tkinter, PySimpleGUIなど）を選定**
  - E-Inkディスプレイ(`epaper_display.py`)による簡易表示に変更?
- [ ] **[★★★☆☆] ユーザーとの対話履歴を表示する、基本的なチャットウィンドウを作成**
- [ ] **[★★★★☆] Notionから日付に紐づくイベントを取得し、カレンダー形式で表示するコンポーネントを実装**

---

## Phase 2: 拡張機能と運用の安定化

アプリケーションをより便利にし、継続的な開発を容易にする段階です。

- [ ] **[★★★☆☆] TimeTree APIの仕様を調査し、イベント取得機能を実装**
- [ ] **[★★★★☆] LINE Messaging APIなどを利用し、受信した画像をNotionにアップロードする機能を実装**
  - `line_controller`の枠組みは実装済み。画像処理ロジックの追加が必要。
- [x] **[★★★☆☆] GitHub Actionsなどを利用したCI/CDパイプラインを構築し、テストの自動化を行う**
  - `.github/workflows/pi-client-ci.yml`にて実装中。

---

## Phase 3: 高度化と発展

アプリケーションをより洗練させ、新たな価値を付加する段階です。

- [ ] **[★★★★☆] `提案機能`としてリストアップした機能の設計と実装**
- [ ] **[★★★★★] `要調査`としてリストアップした項目の実現可能性調査と技術検証**
